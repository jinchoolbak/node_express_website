doctype html
html(lang="ko")
    head
        meta(charset="UTF-8")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        title 웹디자이너 박진출 포트폴리오 웹사이트
        script(src="https://kit.fontawesome.com/a44593fe97.js" crossorigin="anonymous")
        link(rel="stylesheet", href="css/reset.css")
        link(rel="stylesheet", href="css/style.css")
    body 
        include includes/comm_header.pug
        h1(class="d-none") contactList
        main
            div(class="py3")
                h3(class="t-center") Contact & Request Lists
                h4(class="t-center t-upper") 작업요청 및 문의 리스트
            article 
                h2(class="d-none") contactList-main 
                table(class="contact_list container")   
                    tr 
                        th 번호 
                        th 구분
                        th 이름
                        th 연락처
                        th 이메일
                        th 제목
                        th 내용
                        th 등록일
                    each data in dataset 
                        tr 
                            td= data.id
                            td= data.gubun
                            td= data.name
                            td= data.phone
                            td= data.email
                            td= data.title
                            td= data.memo
                            td= data.regdate
                
        include includes/comm_footer.pug